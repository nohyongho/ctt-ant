
# AIRCTT API 개요 문서 (파트너용)

**버전**: 1.0  
**작성일**: 2024년 12월  
**대상**: 벤사/PG, VAN, 제휴사, 데이터 분석사, 마케팅 파트너

---

## 📌 서비스 소개

### AIRCTT (XR/AR 기반 쿠폰/이벤트 플랫폼)

**AIRCTT**는 제휴 매장(가맹점)의 쿠폰, 이벤트, 리뷰 정보를 **표준화된 RESTful API**로 제공하는 플랫폼입니다.

**핵심 목적:**
- VAN/PG사가 결제 시스템과 쉽게 연동할 수 있도록 표준 API 제공
- 데이터 분석사가 매장 데이터를 실시간으로 수집하고 분석 가능
- 마케팅 파트너가 쿠폰/이벤트 정보를 활용하여 타겟 마케팅 수행
- 제휴 매장의 프로모션 정보를 통합 관리하여 고객 경험 향상

**주요 특징:**
- ✅ RESTful API 기반 표준 인터페이스
- ✅ JSON 형식의 명확한 응답 구조
- ✅ 매장, 쿠폰, 이벤트, 리뷰 데이터 통합 제공
- ✅ 실시간 데이터 조회 및 업데이트 지원

---

## 🔌 API 엔드포인트 요약

### Base URL
```
https://your-domain.com/next_api
```

### 인증 방식
- **현재 (DEMO)**: 인증 없음
- **상용 환경**: JWT Bearer Token 방식 (추후 적용)

### 공통 응답 포맷

#### ✅ 성공 응답
```json
{
  "success": true,
  "data": { ... }
}
```

#### ❌ 에러 응답
```json
{
  "success": false,
  "errorMessage": "에러 설명 메시지",
  "errorCode": "ERROR_CODE"
}
```

---

## 📡 API 엔드포인트 상세

### 1. 매장 상세 정보 조회

**엔드포인트**: `GET /next_api/stores/{id}`

**설명**: 특정 매장의 상세 정보를 조회합니다. 매장 기본 정보와 함께 이미지, 진행 중인 이벤트, 사용 가능한 쿠폰, 최근 리뷰를 포함합니다.

**요청 파라미터**:

| 파라미터 | 위치 | 타입 | 필수 | 설명 |
|---------|------|------|------|------|
| `id` | Path | String | ✅ | 매장 고유 ID (예: `store-001`) |

**응답 필드**:

| 필드명 | 타입 | 설명 |
|--------|------|------|
| `id` | String | 매장 고유 ID |
| `name` | String | 매장 이름 |
| `category` | String | 매장 카테고리 (예: 카페, 편의점, 식당) |
| `description` | String | 매장 설명 |
| `rating_avg` | Number | 평균 평점 (0.0 ~ 5.0) |
| `review_count` | Number | 총 리뷰 개수 |
| `address` | String | 매장 주소 |
| `phone` | String | 매장 전화번호 |
| `open_hours` | String | 영업 시간 |
| `lat` | Number | 위도 (지도 표시용) |
| `lng` | Number | 경도 (지도 표시용) |
| `images` | Array | 매장 이미지 목록 |
| `active_events` | Array | 진행 중인 이벤트 목록 |
| `available_coupons` | Array | 사용 가능한 쿠폰 목록 |
| `recent_reviews` | Array | 최근 리뷰 목록 |

**응답 예시**:
```json
{
  "success": true,
  "data": {
    "id": "store-001",
    "name": "AIRCTT 제휴 카페 1",
    "category": "카페",
    "description": "AIRCTT 제휴 카페입니다. 프리미엄 커피와 디저트를 즐기실 수 있습니다.",
    "rating_avg": 4.7,
    "review_count": 45,
    "address": "서울특별시 강남구 테헤란로 123",
    "phone": "02-1234-5678",
    "open_hours": "매일 09:00 ~ 22:00",
    "lat": 37.5012,
    "lng": 127.0396,
    "images": [
      {
        "id": "img-001",
        "image_url": "https://example.com/image1.jpg",
        "is_primary": true
      }
    ],
    "active_events": [...],
    "available_coupons": [...],
    "recent_reviews": [...]
  }
}
```

---

### 2. 매장 쿠폰 목록 조회

**엔드포인트**: `GET /next_api/stores/{id}/coupons`

**설명**: 특정 매장에서 제공하는 쿠폰 목록을 조회합니다. 할인율, 할인 금액, 사은품 등 다양한 형태의 쿠폰 정보를 제공합니다.

**요청 파라미터**:

| 파라미터 | 위치 | 타입 | 필수 | 설명 |
|---------|------|------|------|------|
| `id` | Path | String | ✅ | 매장 고유 ID |
| `status` | Query | String | ❌ | 쿠폰 상태 필터 (`active`, `scheduled`, `expired`) |

**응답 필드**:

| 필드명 | 타입 | 설명 |
|--------|------|------|
| `id` | String | 쿠폰 고유 ID |
| `store_id` | String | 매장 ID |
| `title` | String | 쿠폰 제목 |
| `description` | String | 쿠폰 설명 |
| `discount_type` | String | 할인 타입 (`percent`: 퍼센트, `amount`: 금액, `gift`: 사은품) |
| `discount_value` | Number | 할인 값 (퍼센트: 15, 금액: 5000, 사은품: 0) |
| `start_at` | String | 쿠폰 시작 일시 (ISO 8601) |
| `end_at` | String | 쿠폰 종료 일시 (ISO 8601) |
| `status` | String | 쿠폰 상태 (`active`, `scheduled`, `expired`) |
| `terms_conditions` | String | 이용 약관 |
| `max_usage_count` | Number | 최대 사용 가능 횟수 |
| `current_usage_count` | Number | 현재 사용된 횟수 |

**응답 예시**:
```json
{
  "success": true,
  "data": [
    {
      "id": "coupon-001",
      "store_id": "store-001",
      "title": "전체 메뉴 15% 할인",
      "description": "AIRCTT 결제 시 모든 음료 및 디저트 15% 할인 혜택!",
      "discount_type": "percent",
      "discount_value": 15,
      "start_at": "2024-12-19T00:00:00Z",
      "end_at": "2025-01-18T23:59:59Z",
      "status": "active",
      "terms_conditions": "AIRCTT 결제 시에만 적용 / 다른 쿠폰과 중복 사용 불가",
      "max_usage_count": 100,
      "current_usage_count": 23
    },
    {
      "id": "coupon-002",
      "store_id": "store-001",
      "title": "3,000원 즉시 할인",
      "description": "15,000원 이상 구매 시 3,000원 즉시 할인!",
      "discount_type": "amount",
      "discount_value": 3000,
      "start_at": "2024-12-19T00:00:00Z",
      "end_at": "2025-01-18T23:59:59Z",
      "status": "active",
      "terms_conditions": "최소 주문 금액 15,000원 이상",
      "max_usage_count": 80,
      "current_usage_count": 34
    }
  ]
}
```

**할인 타입 설명**:
- `percent`: 퍼센트 할인 (예: 15% 할인 → `discount_value: 15`)
- `amount`: 금액 할인 (예: 3,000원 할인 → `discount_value: 3000`)
- `gift`: 사은품/증정 (예: 아메리카노 1잔 무료 → `discount_value: 0`)

---

### 3. 매장 이벤트 목록 조회

**엔드포인트**: `GET /next_api/stores/{id}/events`

**설명**: 특정 매장에서 진행 중인 이벤트 목록을 조회합니다. 할인 이벤트, 프로모션, 특별 이벤트 등의 정보를 제공합니다.

**요청 파라미터**:

| 파라미터 | 위치 | 타입 | 필수 | 설명 |
|---------|------|------|------|------|
| `id` | Path | String | ✅ | 매장 고유 ID |
| `status` | Query | String | ❌ | 이벤트 상태 필터 (`active`, `scheduled`, `ended`) |

**응답 필드**:

| 필드명 | 타입 | 설명 |
|--------|------|------|
| `id` | String | 이벤트 고유 ID |
| `store_id` | String | 매장 ID |
| `title` | String | 이벤트 제목 |
| `description` | String | 이벤트 설명 |
| `event_type` | String | 이벤트 타입 (`discount`, `promotion`, `special`) |
| `discount_info` | String | 할인 정보 요약 |
| `start_at` | String | 이벤트 시작 일시 (ISO 8601) |
| `end_at` | String | 이벤트 종료 일시 (ISO 8601) |
| `status` | String | 이벤트 상태 (`active`, `scheduled`, `ended`) |
| `banner_image_url` | String | 이벤트 배너 이미지 URL |

**응답 예시**:
```json
{
  "success": true,
  "data": [
    {
      "id": "event-001",
      "store_id": "store-001",
      "title": "💳 AIRCTT 결제 시 포인트 2배 적립",
      "description": "AIRCTT 결제 시스템으로 결제하시면 기본 적립 포인트에 추가로 2배 적립!",
      "event_type": "promotion",
      "discount_info": "AIRCTT 결제 시 포인트 2배 적립",
      "start_at": "2024-12-19T00:00:00Z",
      "end_at": "2025-01-18T23:59:59Z",
      "status": "active",
      "banner_image_url": "https://example.com/event-banner.jpg"
    }
  ]
}
```

**이벤트 타입 설명**:
- `discount`: 할인 이벤트
- `promotion`: 프로모션 이벤트 (포인트 적립, 사은품 증정 등)
- `special`: 특별 이벤트 (시즌 이벤트, 기념일 이벤트 등)

---

### 4. 매장 리뷰 목록 조회

**엔드포인트**: `GET /next_api/stores/{id}/reviews`

**설명**: 특정 매장의 고객 리뷰 목록을 조회합니다. 페이지네이션과 정렬 기능을 지원합니다.

**요청 파라미터**:

| 파라미터 | 위치 | 타입 | 필수 | 설명 |
|---------|------|------|------|------|
| `id` | Path | String | ✅ | 매장 고유 ID |
| `limit` | Query | Number | ❌ | 페이지당 개수 (기본값: 10) |
| `offset` | Query | Number | ❌ | 오프셋 (기본값: 0) |
| `sort` | Query | String | ❌ | 정렬 기준 (`recent`, `rating_high`, `rating_low`) |

**응답 필드**:

| 필드명 | 타입 | 설명 |
|--------|------|------|
| `id` | String | 리뷰 고유 ID |
| `store_id` | String | 매장 ID |
| `user_id` | String | 작성자 ID |
| `rating` | Number | 별점 (1 ~ 5) |
| `content` | String | 리뷰 내용 |
| `images` | Array | 리뷰 이미지 URL 배열 |
| `created_at` | String | 작성 일시 (ISO 8601) |
| `user_name` | String | 작성자 이름 |
| `user_avatar` | String | 작성자 프로필 이미지 URL |

**응답 예시**:
```json
{
  "success": true,
  "data": [
    {
      "id": "review-001",
      "store_id": "store-001",
      "user_id": "user-001",
      "rating": 5,
      "content": "AIRCTT 결제 시스템이 정말 편리하고 빨라요! 커피 맛도 훌륭합니다.",
      "images": ["https://example.com/review1.jpg"],
      "created_at": "2024-12-17T00:00:00Z",
      "user_name": "김민수",
      "user_avatar": "https://example.com/avatar1.jpg"
    },
    {
      "id": "review-002",
      "store_id": "store-001",
      "user_id": "user-002",
      "rating": 4,
      "content": "분위기도 좋고 디저트도 맛있어요!",
      "images": [],
      "created_at": "2024-12-14T00:00:00Z",
      "user_name": "이지은",
      "user_avatar": "https://example.com/avatar2.jpg"
    }
  ]
}
```

---

## 🔐 에러 코드 및 처리

### HTTP 상태 코드

| 상태 코드 | 설명 |
|----------|------|
| `200` | 성공 |
| `201` | 생성 성공 (POST 요청) |
| `400` | 잘못된 요청 (파라미터 오류) |
| `404` | 리소스를 찾을 수 없음 |
| `500` | 서버 내부 오류 |

### 에러 코드 목록

| 에러 코드 | 설명 | HTTP 상태 |
|----------|------|-----------|
| `STORE_NOT_FOUND` | 매장을 찾을 수 없음 | 404 |
| `INVALID_INPUT` | 잘못된 입력 데이터 | 400 |
| `INTERNAL_SERVER_ERROR` | 서버 내부 오류 | 500 |

**에러 응답 예시**:
```json
{
  "success": false,
  "errorMessage": "Store not found: store-999",
  "errorCode": "STORE_NOT_FOUND"
}
```

---

## 🧪 API 테스트 방법

### 브라우저에서 테스트
```javascript
// 매장 상세 조회
fetch('/next_api/stores/store-001')
  .then(r => r.json())
  .then(console.log);

// 쿠폰 목록 조회
fetch('/next_api/stores/store-001/coupons?status=active')
  .then(r => r.json())
  .then(console.log);

// 이벤트 목록 조회
fetch('/next_api/stores/store-001/events')
  .then(r => r.json())
  .then(console.log);

// 리뷰 목록 조회
fetch('/next_api/stores/store-001/reviews?limit=10')
  .then(r => r.json())
  .then(console.log);
```

### cURL 명령어
```bash
# 매장 상세 조회
curl https://your-domain.com/next_api/stores/store-001

# 쿠폰 목록 조회
curl "https://your-domain.com/next_api/stores/store-001/coupons?status=active"

# 이벤트 목록 조회
curl https://your-domain.com/next_api/stores/store-001/events

# 리뷰 목록 조회
curl "https://your-domain.com/next_api/stores/store-001/reviews?limit=10&sort=recent"
```

---

## 📋 DEMO 환경 안내

### 현재 상태
- **환경**: Zoer 기반 데모 환경
- **데이터**: Mock(샘플) 데이터 사용
- **저장소**: 브라우저 localStorage 기반
- **인증**: 인증 없음 (오픈 API)

### DEMO 매장 샘플
현재 데모 환경에서는 다음 3개의 AIRCTT 제휴 매장 샘플을 제공합니다:

| 매장 ID | 매장 이름 | 카테고리 | 설명 |
|---------|----------|---------|------|
| `store-001` | AIRCTT 제휴 카페 1 | 카페 | 프리미엄 커피와 디저트 제공 |
| `store-002` | AIRCTT 제휴 편의점 1 | 편의점 | 24시간 영업, 다양한 생필품 판매 |
| `store-003` | AIRCTT 제휴 식당 1 | 한식당 | 신선한 재료로 만든 건강한 한식 |

### 상용 환경 전환 계획
실제 상용 환경에서는 다음과 같이 변경될 예정입니다:

**인프라**:
- ✅ AWS 또는 Cloudflare 기반 API 서버로 이전
- ✅ PostgreSQL 데이터베이스 사용
- ✅ CDN을 통한 이미지 제공
- ✅ Redis 캐싱 적용

**보안**:
- ✅ JWT Bearer Token 인증 방식 적용
- ✅ API Rate Limiting (요청 제한)
- ✅ HTTPS 필수
- ✅ API Key 발급 및 관리

**성능**:
- ✅ 응답 시간 최적화 (평균 100ms 이하)
- ✅ 동시 접속 처리 능력 향상
- ✅ 실시간 데이터 동기화

---

## 🚀 연동 가이드

### VAN/PG사 연동 시나리오
1. **결제 시점**: 고객이 AIRCTT 결제 시스템으로 결제 시도
2. **쿠폰 조회**: `GET /stores/{id}/coupons` API로 사용 가능한 쿠폰 조회
3. **할인 적용**: 쿠폰 정보를 기반으로 결제 금액 할인 적용
4. **결제 완료**: 할인된 금액으로 결제 처리
5. **포인트 적립**: 이벤트 정보를 기반으로 추가 포인트 적용

### 데이터 분석사 활용 시나리오
1. **매장 데이터 수집**: 정기적으로 매장 상세 정보 조회
2. **쿠폰 효과 분석**: 쿠폰 사용 현황 (`current_usage_count`) 모니터링
3. **리뷰 감성 분석**: 리뷰 데이터를 수집하여 고객 만족도 분석
4. **트렌드 파악**: 이벤트 타입별 효과 분석

### 마케팅 파트너 활용 시나리오
1. **타겟 쿠폰 발송**: 특정 카테고리 매장의 쿠폰 정보 수집
2. **이벤트 알림**: 진행 중인 이벤트 정보를 고객에게 푸시 알림
3. **개인화 추천**: 고객 위치 기반 근처 매장 쿠폰 추천
4. **효과 측정**: 쿠폰 사용률 및 이벤트 참여율 추적

---

## 📞 기술 지원

### 문의 채널
- **이메일**: api-support@airctt.com
- **기술 문서**: https://docs.airctt.com
- **개발자 포럼**: https://forum.airctt.com

### 파트너 등록
상용 환경 API 사용을 위해서는 파트너 등록이 필요합니다:
1. 파트너 신청서 제출
2. API Key 발급
3. 테스트 환경 접근 권한 부여
4. 상용 환경 전환

---

## 📊 API 현황 요약

| 항목 | 현재 (DEMO) | 상용 환경 (예정) |
|------|------------|----------------|
| **Base URL** | `/next_api` | `https://api.airctt.com/v1` |
| **인증** | 없음 | JWT Bearer Token |
| **데이터 소스** | localStorage (Mock) | PostgreSQL |
| **응답 시간** | ~50ms | ~100ms (목표) |
| **Rate Limit** | 없음 | 1000 req/min |
| **HTTPS** | 선택 | 필수 |

---

## 📝 변경 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|----------|
| 1.0 | 2024-12 | 초기 버전 작성 (DEMO 환경 기준) |

---

**© 2024 AIRCTT. All rights reserved.**

이 문서는 AIRCTT 파트너사를 위한 기술 문서입니다. 상용 환경 전환 시 내용이 업데이트될 수 있습니다.
